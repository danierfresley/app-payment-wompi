{
  "name": "product-checkout-app",
  "private": true,
  "scripts": {
    "test": "npm --prefix backend test -- --coverage --watchAll=false && npm --prefix frontend test -- --coverage --watchAll=false",
    "dev:api": "npm --prefix backend run start:dev",
    "dev:web": "npm --prefix frontend run dev"
  }
}
